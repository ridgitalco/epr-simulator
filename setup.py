from setuptools import setup

setup(
    name="epr",
    version="0.1.0",
    packages=[],
    include_package_data=False,
    zip_safe=False,
    python_requires=">=3.13",
    install_requires=[],
)
